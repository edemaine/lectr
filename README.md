# Lectr: Synchronized Lecture Videos and Notes

Build instructions:
* `npm install` for first build (to install dependencies)
* `npm run prepare` suffices for future builds

Running test server:
* `npm start` will run `python testserver.py`
